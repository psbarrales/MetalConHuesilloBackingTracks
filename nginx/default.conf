server {
  listen ${PORT};
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  location /audio/ {
    add_header Cache-Control "public, max-age=3600";
    try_files $uri =404;
  }
}
